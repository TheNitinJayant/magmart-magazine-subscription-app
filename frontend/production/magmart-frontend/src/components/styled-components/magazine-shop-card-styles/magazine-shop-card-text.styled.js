import styled from "styled-components";

const textBackgroundColorHex = "#3F3F4E";
const color = "white";

export const StyledMagazineShopCardText = styled.div`
  background-color: ${textBackgroundColorHex};
  color: ${color};
  border-radius: 4px;
  min-width: 2.5rem;
  padding: 0.5rem;
  
  margin: 1rem;
`;