const ROOT = "http://localhost:8080";

const SIGN_UP = `${ROOT}/signup`;
const SIGN_IN = `${ROOT}/signin`;
const SUBSCRIPTIONS = `${ROOT}/subscriptions`;
const MAGAZINES = `${ROOT}/magazines`;
const USERS = `${ROOT}/users`;
const SHOP = `${ROOT}/showsubscription`;

const SHOWCART = `${ROOT}/showcart`
const SUBSCRIBED = `${ROOT}/showsubscribed`

export { SIGN_UP, SIGN_IN, SUBSCRIPTIONS, USERS, MAGAZINES,SHOWCART,SUBSCRIBED,SHOP  };

